{
  "name": "software-project-main",
  "private": true,
  "for 
  data_/// app.console
  "version run - 0.001
  ip_address == 192.168.100.222 
  //tmade by taha imhamed 
  //the server keys will be giveng there in the office 
  // the internal server ip address is here v192.168.100.222   take it and make the v1 and the v2 till v 0.23 
  //with the app 
  "
  "version": "1.0.0",
  "scripts": {
    "predev": "node scripts/bootstrap-dev.mjs",
    "setup": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently -n backend,frontend -c blue,green \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
 



